import 'package:flutter/material.dart';

class CustomTheme {
  static Color homeScreenButtonColor = const Color.fromRGBO(188, 206, 248, 1.0);
  static Color borderColor = const Color.fromRGBO(81, 127, 233, 1.0);
  static BorderSide nonBottom = BorderSide(color: borderColor, width: 5);
  static double marginBottom3DEffect = 10;
  static Duration buttonTapAnimationDuration =
      const Duration(milliseconds: 100);
}
